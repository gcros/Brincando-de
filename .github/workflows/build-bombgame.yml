name: Build BombGame APK

on:
  push:
    branches: [main]
    paths:
      - '.bombgame_bundle/**'
      - '.github/workflows/build-bombgame.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Android 35
        shell: bash
        run: |
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          if [ ! -x "$SDKMANAGER" ]; then
            SDKMANAGER="$(find "$ANDROID_HOME/cmdline-tools" -type f -name sdkmanager | sort | tail -n 1)"
          fi
          yes | "$SDKMANAGER" --licenses >/dev/null || true
          "$SDKMANAGER" "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Rebuild Android project
        run: |
          base64 -d .bombgame_bundle/project.zip.b64 > /tmp/BombGame_Android.zip
          unzip -q /tmp/BombGame_Android.zip -d .
          test -f BombGame_Android/app/src/main/AndroidManifest.xml

      - name: Remove AndroidX dependency
        shell: bash
        run: |
          sed -i "/androidx.appcompat:appcompat/d" BombGame_Android/app/build.gradle
          sed -i 's/import androidx.appcompat.app.AppCompatActivity;/import android.app.Activity;/' BombGame_Android/app/src/main/java/com/fanhouse/bombgame/MainActivity.java
          sed -i 's/extends AppCompatActivity/extends Activity/' BombGame_Android/app/src/main/java/com/fanhouse/bombgame/MainActivity.java
          sed -i 's#parent="Theme.AppCompat.DayNight.NoActionBar"#parent="@android:style/Theme.Material.NoActionBar"#' BombGame_Android/app/src/main/res/values/themes.xml

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Build debug APK
        run: gradle -p BombGame_Android assembleDebug --stacktrace

      - name: Prepare APK
        run: cp BombGame_Android/app/build/outputs/apk/debug/app-debug.apk BombGame.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: BombGame-APK
          path: BombGame.apk
          if-no-files-found: error
